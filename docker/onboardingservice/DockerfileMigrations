# Используем официальный образ migrate
FROM migrate/migrate

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем файлы миграций в контейнер
COPY ./docker/onboardingservice/migrations /migrations

# Команда по умолчанию
CMD ["tail", "-f", "/dev/null"]