syntax = "proto3";

package users.v1;

import "errors/errors.proto";

option go_package = "scoresstatsservice/scores/v1;v1";

enum Reason {
  option (errors.default_code) = 500;

  INVALID_PARAMETERS = 0 [(errors.code) = 400];

}
