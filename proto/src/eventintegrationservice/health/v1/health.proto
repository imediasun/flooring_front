syntax = "proto3";

package health.v1;

import "google/api/annotations.proto";
import "google/protobuf/empty.proto";

option go_package = "/eventintegrationservice/health/v1;v1";

// The Health service definition.
service Health {
  rpc Check (google.protobuf.Empty) returns (CheckReply) {
    option (google.api.http) = {
			get: "/v1/health"
    };
  };
}

message CheckReply {
  string status = 1;
  string version = 2;
  string env = 3;
  string postgres = 4;
}
